const a = 5;

console.log(a);

const sumOfNumbers = [1, 2, 3, 4, 5, 6, 7].reduce(
  (prev, curr) => prev + curr,
  0,
);

const test = [123].map((item) => item);

const test1 = [{}, {}];
const name = "rokas";
